apiVersion: v1
kind: Service
metadata:
  name: lineagelens-proxy
  namespace: lineagelens
  labels:
    app: lineagelens-proxy
spec:
  selector:
    app: lineagelens-proxy
  ports:
    - name: http
      port: 8788
      targetPort: 8788
      protocol: TCP
  type: ClusterIP
