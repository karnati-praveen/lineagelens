# Core — required for all modes
# FastAPI bumped from 0.115.6: 0.115.x pins starlette<0.42.0 which is vulnerable
# to CVE-2025-54121, CVE-2025-62727, PYSEC-2026-161. FastAPI 0.116.0+ unlocks
# starlette 0.42.x which contains the fixes.
fastapi>=0.116.0
starlette>=0.42.0
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
PyJWT[crypto]>=2.9.0
pydantic-settings==2.7.0
# Bumped from 1.0.1: CVE-2026-28684 (path traversal in dotenv parsing).
python-dotenv>=1.2.2
httpx>=0.27.0
pyarrow>=14.0.0

# Lite mode (SQLite) — zero external DB deps
aiosqlite>=0.20.0

# Postgres mode
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6

# Graph lineage (Max mode) — import is lazy; safe to install in all modes
neo4j==5.26.0

# Redis — rate limiter and shared KV store (export jobs, webhooks)
redis[asyncio]>=5.0.0

# JSON Schema validation — spec conformance tests
jsonschema>=4.23.0

# AST parsing — optional, improves code analysis accuracy
# tree-sitter==0.21.3; python_version < "3.12"
# tree-sitter-languages==1.10.2; python_version < "3.12"
